{
  "name": "tipscoach-reports",
  "private": true,
  "description": "Tipscoach public reports — static site served via Cloudflare Workers",
  "scripts": {
    "build": "bash ./scripts/build.sh",
    "test": "bash ./scripts/test.sh",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
